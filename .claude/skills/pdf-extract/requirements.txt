# pdf-extract dependencies (Python 3.10+). Install:
#   python -m pip install -r .claude/skills/pdf-extract/requirements.txt
pymupdf4llm>=0.0.17     # fallback engine + PDF probing (pulls pymupdf)
pdfplumber>=0.11        # auxiliary table inspection
docling>=2.100          # PRIMARY engine: layout + TableFormer + OCR
rapidocr-onnxruntime>=1.3   # OCR engine for scanned / image-table pages (CPU, ONNX)
onnxruntime>=1.17       # rapidocr backend
